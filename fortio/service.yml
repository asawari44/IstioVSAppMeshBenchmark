apiVersion: v1
kind: Service
metadata:
  namespace: fortio
  name: fortio
  labels:
    app: fortio
    service: fortio
spec:
  ports:
  - port: 8080
    name: http
    targetPort: 8080
  - port: 8079
    name: http2
    targetPort: 8079
  selector:
    app: fortio
  type: LoadBalancer